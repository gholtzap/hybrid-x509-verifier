# Python cryptography X.509 adapter

The two images run cryptography 49.0.0, used by the paper, and current version 50.0.0. The
Python base image uses a fixed digest. All Python packages use exact versions and wheel hashes.
