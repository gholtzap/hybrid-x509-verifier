# OpenSSL X.509 adapter

This image builds OpenSSL 4.0.1 from the official source archive. The source archive, build
image, and runtime image use fixed digests. The runtime has no package installation step.

```sh
tools/openssl-x509-adapter/build.sh
```
